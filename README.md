# KyberSpace
AI-governed hardware gatekeeper that ensures only you can enter your car, even in a post-quantum world.
# QuickStart
```
python3 -m venv venv
source venv/bin/activate && pip install google-cloud-aiplatform google-adk liboqs-python
```